{
  "name": "maths-suite-payments",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: Stripe payment backend for Maths Suite",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
